.next/
out/
build/
coverage/
node_modules/
playwright-report/
test-results/
pnpm-lock.yaml
next-env.d.ts
# Generated by drizzle-kit; re-running `pnpm db:generate` would just
# reformat these back to its own style, so formatting them here only adds
# diff noise.
drizzle/

# Vendored agent skill packs — third-party Markdown this project does not own
# and must not reformat. 142 of its files fail `format:check` otherwise.
.agents/
